# 我的 mini ML library

這個資料夾是獨立 Python package，`src/mini_ml` 放實作，`tests` 放自寫測試。
`uv sync` 安裝環境與 package；notebook 使用此環境的 Python kernel。

請逐週補上：完成的函式、執行方式、資料來源、結果與限制。
正式驗收使用 repo 根目錄 `tools/course.py test`，不只檢查這裡的自寫測試。


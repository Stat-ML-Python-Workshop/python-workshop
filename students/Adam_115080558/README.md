# workshop-mini-ml

Week 1 assignment for Stat-ML-Python-Workshop.

## Completed Tasks
- Initialized installable Python package structure under `src/mini_ml/`.
- Configured project metadata and dependencies with `pyproject.toml` using setuptools.
- Implemented `hello_world()` public API in `src/mini_ml/greetings.py`.
- Added unit test in `tests/test_greetings.py`.
- Synchronized local development environment and locked dependencies using `uv`.

## Run Tests
To run the test suite:
```powershell
uv run pytest
```

